'use client';

import { QueryProcessor } from '@/components/query/query-processor';

export default function QueriesPage() {
  return (
    <div className="h-full">
      <QueryProcessor />
    </div>
  );
}
